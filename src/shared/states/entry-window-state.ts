import { EntryWindowMode } from '_shared/enums';
import { EntryWindowState } from '_shared/types/state/EntryWindowState';

export const entryWindowState: EntryWindowState = {
  entry: undefined,
  mode: EntryWindowMode.View,
};
